widget.Spacer(length = 8),
   widget.KeyboardLayout(
                 foreground = colors[4],
                 configured_keyboards = ['gb'],
                 fmt = '⌨  Kbd: {}',
                 decorations=[
                     BorderDecoration(
                         colour = colors[4],
                         border_width = [0, 0, 2, 0],
                     )
                 ],
                 ),
        widget.Spacer(length = 8),



   Key([mod], "r", lazy.spawncmd(), desc="Spawn a command using a prompt widget"),
Dependencies:

xorg xorg-xinit xorg-xsetroot picom alacritty qtile copyq nitrogen code brave rofi
Find the good theme for youself
for qtile use aur for qtile-extras psutil 
pulse utils for the volume to work

